var assert  = require('assert');

describe('AWESOM0', function () {
  it('can be imported without blowing up', function () {
    var awesom0 = require('../awesom0');
    assert(awesom0 !== undefined);
  });
});
